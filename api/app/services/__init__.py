"""Service layer — business logic above the ORM (CRUD, ledger, recurrence).

Wave 2 domain agents add one module per domain here, e.g. ``students.py``,
``scheduling.py``, ``credits.py``, ``payments.py``. Keep all DB access
org-scoped (filter by ``org_id`` on every query).
"""
